Perform an independent, read-only review of SPA Milestone 1 Plan 5 Task 1 (Additive Milestone 1 migration and two-path migration tests) in C:\GitHub\backendtest.

Verify: the additive Alembic revision 20260904_02 (file migrations/versions/20260904_02_investment_operating_system_m1.py) creates only the exact table set in migrations/m1_table_inventory.py's M1_TABLES constant, in FK-safe order, with no alteration to any legacy table. That set is 20 tables: Task 1's original frozen 18 (business_group, company, user_entitlement, research_revision, research_point, ownership_snapshot, governance_flag, company_disclosure, market_plan_revision, forecast_revision, forecast_line, valuation_revision, valuation_reference_line, institution, document, document_company_link, institutional_report_metadata, document_audit_event) plus document_content and document_storage_location. Those two are a human-authorized, explicitly documented superseding change to Task 1's frozen list (see the "Approved amendment (2026-09-25)" note under Task 1 Step 2 in the governing plan, and the docstring in migrations/m1_table_inventory.py) because the Plan 4 content-addressed document storage refactor -- already implemented and independently reviewed -- added them after Task 1's table list was frozen, and this revision's own legacy-column migration step requires both to exist. Do not flag the 20-table set, the migration filename, or this amendment as a defect; they are already decided. Do flag it if the migration or tests create/drop any table outside that 20-table set, or if the amendment note or m1_table_inventory.py is missing/inconsistent.

Also verify: the fresh-database upgrade path and the existing-schema upgrade path (stamp 20260904_01, snapshot legacy, upgrade head) both pass and assert byte-identical legacy snapshots -- including that the fresh path itself proves legacy tables are unchanged by snapshotting them before 20260904_02 is applied, not only after; and the migration-facing schema invariants hold (one (user_id, product_code) entitlement row, nullable forecast_line.eps, valuation-stream uniqueness, valuation reference-line ordering, non-null/unique document fingerprint, document/company-link uniqueness, institution uniqueness, document-audit relationships, and that the filtered/partial unique indexes on document.metadata_fingerprint and document_company_link.document_id actually carry a WHERE predicate rather than merely being unique).

Inspect the actual migration file, migrations/m1_table_inventory.py, tests/migrations/test_milestone1_fresh_upgrade.py, tests/migrations/test_milestone1_existing_schema_upgrade.py, tests/migrations/helpers.py, and the governing plan document (docs/superpowers/plans/2026-09-04-investment-operating-system-m1-api-seed-regression.md in the sibling stock-price-alert repo -- Task 1 is the first task in that document, including its Step 2 amendment note). Ignore unrelated later hygiene or tooling changes.

Run the migration tests if this read-only sandbox permits writable temporary storage; report NOT PROVEN with blocking evidence if not.

Make no edits, commits, or pushes. Do not close P5T1.

End with exactly one review verdict on its own line:
SAFE
SAFE WITH NON-BLOCKING OBSERVATIONS
CHANGES REQUIRED
